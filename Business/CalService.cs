using Interface;

namespace Business
{
   // public class CalService : ICalService
    //{


    //    public double Calculate(string expression)
    //    {
    //        string[] n = System.Text.RegularExpressions.Regex.Split(expression, @"([+\-*/])");
    //        double total = double.Parse(n[0]);

    //        for (int i = 1; i < n.Length; i += 2)
    //        {
    //            string op = n[i];
    //            double next = double.Parse(n[i + 1]);

    //            if (op == "+") total += next;
    //            if (op == "-") total -= next;
    //            if (op == "*") total *= next;
    //            if (op == "/") total = (next != 0) ? (total / next) : double.PositiveInfinity;
    //        }
    //        return total;
    //    }

    //}
    
}
